"use client";
import { styled } from "@mui/material/styles";

export const StyledIframe = styled("iframe")(() => ({
  height: "100%",
  width: "100%",
  border: 0,
  position: "fixed",
  left: 0,
  top: 0,
}));
