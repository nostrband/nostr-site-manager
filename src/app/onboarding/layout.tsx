export const metadata = {
  title: "Nostr Blog | Onboarding",
  description: "App for managment your site",
};

export default async function OnboardingLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
