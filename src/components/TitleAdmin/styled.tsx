"use client";
import { styled } from "@mui/material/styles";
import { Typography } from "@mui/material";

export const StyledTitleAdmin = styled(Typography)(() => ({
  fontWeight: "bold",
  paddingBottom: 20,
  marginBottom: 20,
  borderBottom: "1px solid #ececec",
}));
