"use client";
import { styled } from "@mui/material/styles";

export const MainWrapper = styled("div")(() => ({
  position: "relative",
  display: "flex",
  minHeight: "100%",
  overflow: "hidden",
}));
